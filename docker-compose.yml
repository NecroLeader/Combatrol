services:
  combatrol:
    build: .
    container_name: combatrol_app
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    networks:
      - worldz_net
    expose:
      - "8000"

networks:
  worldz_net:
    external: true
    name: worldz_net
